/** Single source of truth for the one-time unlock purchase. */

/** App Store product identifier for unlocking one calculation. */
export const UNLOCK_PRODUCT_ID = "com.mrsolardoc.calculation.unlock";

/** Displayed fallback price. The App Store price is always authoritative. */
export const UNLOCK_PRICE_AMOUNT = 49;
export const UNLOCK_PRICE_CURRENCY = "SEK";
